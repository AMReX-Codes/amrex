# Even refinement ratio with a level 1 blocking factor larger than
# ref_ratio times the level 0 blocking factor.  Level 0 grids are then
# not aligned with the coarsened tags; the grids must still be correct.
geometry.prob_lo = 0. 0. 0.
geometry.prob_hi = 1. 1. 1.
geometry.is_periodic = 0 0 0

amr.n_cell = 64 64 32
amr.max_level = 1
amr.ref_ratio = 2
amr.blocking_factor = 8 32
amr.max_grid_size = 32 32

test.centers = 0.3 0.4 0.5   0.9 0.95 0.5
test.radius = 0.2
