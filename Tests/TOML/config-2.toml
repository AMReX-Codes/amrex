# This is included in config.toml. It has its own table environment.

config-2.x = 10

[config-2]
y = 20
z = 30

[invalid] # This is invalid. We use it to test error handling
empty_array = []
invalid_array = [1,foo]

