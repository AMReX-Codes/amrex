After manually changing the contents of the BoxLib_Version.txt file

echo "0.1.5" > BoxLib_Version.txt

committing that changes, then tagging a version:

git tag -a v0.1.5 -m "Version 0.1.5"

Generate a taball with 

git archive --format=tar --prefix=ccse-0.1.5/ v0.1.5 | gzip >../ccse-0.1.5.tar.gz

